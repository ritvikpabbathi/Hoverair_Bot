# Free at console.groq.com (API Keys in the sidebar). Used for talk/chat
# answer generation, talk-mode speech-to-text (Whisper), and talk-mode
# text-to-speech (Orpheus). The Orpheus TTS model additionally requires
# accepting its terms once in the Groq console before /api/tts will work —
# see README.md.
GROQ_API_KEY=
